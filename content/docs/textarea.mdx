---
title: Textarea
description: Form field for multi-line text entry.
---

import { Svelte } from "@/components/svelte";
import { Tabs, Tab } from "@/components/tabs";
import { Step, Steps } from "@/components/steps";
import { TypeTable } from "@/components/type-table";
import { DocsButton } from "@/components/ui/docs-button";
import { DocsTabs, DocsTab } from "@/components/docs-tabs";

<div className="h-6" />

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/textarea" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Textarea } from '$lib/components/textarea';
</script>

<Textarea placeholder="Type your message here" />
```

  </DocsTab>
</DocsTabs>

## Usage

```svelte
<script lang="ts">
  import { Textarea } from '$lib/components/textarea';
</script>
```

```svelte
<Textarea placeholder="Type your message here" />
```

## Properties

<TypeTable
  type={{
    placeholder: {
      description: "The placeholder text for the textarea.",
      type: "string",
    },
    disabled: {
      description: "Whether the textarea is disabled.",
      type: "boolean",
    },
    error: {
      description: "Whether the textarea has an error.",
      type: "boolean",
    },
    value: {
      description: "The value of the textarea.",
      type: "string",
    },
  }}
/>

## Examples

### Error

Use the `error` property to indicate that the textarea has an error.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/textarea/error" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Textarea } from '$lib/components/textarea';
</script>

<Textarea error placeholder="Type your message here" />
```

  </DocsTab>
</DocsTabs>

### Disabled

Use the `disabled` property to prevent users from interacting with the textarea.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/textarea/disabled" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Textarea } from '$lib/components/textarea';
</script>

<Textarea disabled placeholder="Write your message here..." />
```

  </DocsTab>
</DocsTabs>
