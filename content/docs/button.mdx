---
title: Button
description: Interactive element that initiates an action, with a label or icon describing the outcome.
---

import { Svelte } from "@/components/svelte";
import { Tabs, Tab } from "@/components/tabs";
import { Step, Steps } from "@/components/steps";
import { TypeTable } from "@/components/type-table";
import { DocsButton } from "@/components/ui/docs-button";
import { DocsTabs, DocsTab } from "@/components/docs-tabs";

<DocsButton href="https://bits-ui.com/docs/components/button">
  API Reference
</DocsButton>

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/button" height={360} />
  </DocsTab>
  <DocsTab value="Code">
    
```svelte
<script lang="ts">
  import { Button } from '$lib/components/button';
</script>

<Button variant="primary">Button</Button>
```

  </DocsTab>
</DocsTabs>

## Usage

```svelte
<script lang="ts">
  import { Button } from '$lib/components/button';
</script>
```

```svelte
<Button variant="primary">Button</Button>
```

## Properties

These are the properties specific to our `button` implementation. For the complete list of properties, please refer to their <a href="https://www.bits-ui.com/docs/components/button" target="_blank">documentation</a>.

### Button

<TypeTable
  type={{
    variant: {
      description: "The variant of the button.",
      type: "string",
      typeDescription: "default | primary | critical | outline | ghost",
      default: "default",
    },
    disabled: {
      description: "Whether the button is disabled.",
      type: "boolean",
      default: "false",
    },
    loading: {
      description: "Whether the button is loading.",
      type: "boolean",
      default: "false",
    },
    leadingIcon: {
      description: "The leading icon of the button.",
      type: "ComponentType | null",
      default: "null",
    },
    trailingIcon: {
      description: "The trailing icon of the button.",
      type: "ComponentType | null",
      default: "null",
    },
  }}
/>

### ButtonIcon

<TypeTable
  type={{
    variant: {
      description: "The variant of the button icon.",
      type: "string",
      typeDescription: "default | primary | critical | outline | ghost",
      default: "default",
    },
    size: {
      description: "The size of the button icon.",
      type: "string",
      typeDescription: "sm | md",
      default: "medium",
    },
    disabled: {
      description: "Whether the button is disabled.",
      type: "boolean",
      default: "false",
    },
    loading: {
      description: "Whether the button is loading.",
      type: "boolean",
      default: "false",
    },
  }}
/>

## Examples

### Variants

Use the `variant` property to change the appearance of the button.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/button/variants" height={360} />
  </DocsTab>
  <DocsTab value="Code">
    
```svelte
<script lang="ts">
  import { Button } from '$lib/components/button';
</script>

<Button variant="default">Default</Button>
<Button variant="primary">Primary</Button>
<Button variant="outline">Outline</Button>
<Button variant="ghost">Ghost</Button>
<Button variant="critical">Critical</Button>
```
  </DocsTab>
</DocsTabs>

### Disabled

Use the `disabled` property to disable the button.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/button/disabled" height={360} />
  </DocsTab>
  <DocsTab value="Code">
    
```svelte
<script lang="ts">
  import { Button } from '$lib/components/button';
</script>

<Button disabled variant="default">Default</Button>
<Button disabled variant="primary">Primary</Button>
<Button disabled variant="outline">Outline</Button>
<Button disabled variant="ghost">Ghost</Button>
<Button disabled variant="critical">Critical</Button>
```
  </DocsTab>
</DocsTabs>

### Loading

Use the `loading` property to show a loading indicator.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/button/loading" height={360} />
  </DocsTab>
  <DocsTab value="Code">
    
```svelte
<script lang="ts">
  import { Button } from '$lib/components/button';
</script>

<Button loading variant="default">Button</Button>
<Button loading variant="primary">Button</Button>
<Button loading variant="outline">Button</Button>
<Button loading variant="ghost">Button</Button>
<Button loading variant="critical">Button</Button>
```
  </DocsTab>
</DocsTabs>

### Icon support

Use the `leadingIcon` and `trailingIcon` properties to add an icon to the button.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/button/icon-support" height={360} />
  </DocsTab>
  <DocsTab value="Code">
    
```svelte
<script lang="ts">
  import { Button } from '$lib/components/button';
  import { Add, TabNew } from '$lib/components/icons';
</script>

<Button variant="default" trailingIcon={TabNew} href="https://webflow.com">
	View documentation
</Button>
<Button variant="primary" leadingIcon={Add}>
	Create new project
</Button>
```
  </DocsTab>
</DocsTabs>

### Button icon

Use the `ButtonIcon` component to create icon-only buttons.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/button/icon" height={360} />
  </DocsTab>
  <DocsTab value="Code">
    
```svelte
<script lang="ts">
  import { ButtonIcon } from '$lib/components/button';
  import { Add } from '$lib/components/icons';
</script>

<ButtonIcon variant="default" icon={Add} />
<ButtonIcon variant="primary" icon={Add} />
<ButtonIcon variant="outline" icon={Add} />
<ButtonIcon variant="ghost" icon={Add} />
<ButtonIcon variant="critical" icon={Add} />
```
  </DocsTab>
</DocsTabs>

### Button icon size

Use the `size` property to change the size of the button icon.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/button/icon-size" height={360} />
  </DocsTab>
  <DocsTab value="Code">
    
```svelte
<script lang="ts">
  import { ButtonIcon } from '$lib/components/button';
  import { Add } from '$lib/components/icons';
</script>

<ButtonIcon variant="primary" size="small" icon={Add} />
<ButtonIcon variant="primary" size="medium" icon={Add} />
```
  </DocsTab>
</DocsTabs>

### Link

Use the `href` property to create a link button.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/button/link" height={360} />
  </DocsTab>
  <DocsTab value="Code">
    
```svelte
<script lang="ts">
  import { Button } from '$lib/components/button';
  import { TabNew } from '$lib/components/icons';
</script>

<Button variant="default" trailingIcon={TabNew} href="https://webflow.com">
  Webflow Account
</Button>
```
  </DocsTab>
</DocsTabs>
