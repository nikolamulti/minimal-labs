---
title: Radio group
description: Form control that allows users to select one option from a set of choices.
---

import { Svelte } from "@/components/svelte";
import { Tabs, Tab } from "@/components/tabs";
import { Step, Steps } from "@/components/steps";
import { TypeTable } from "@/components/type-table";
import { DocsButton } from "@/components/ui/docs-button";
import { DocsTabs, DocsTab } from "@/components/docs-tabs";

<DocsButton href="https://bits-ui.com/docs/components/radio-group">
  API Reference
</DocsButton>

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/radio-group" height={360} />
  </DocsTab>
  <DocsTab value="Code">
    
```svelte
<script lang="ts">
  import { RadioGroup, Radio, RadioField } from '$lib/components/radio-group';
  import { Label, Description } from '$lib/components/fieldset';
  
  let selectedPlan = 'personal';
</script>

<RadioGroup value={selectedPlan}>
  <RadioField>
    <Radio value="personal" id="personal" />
    <Label for="personal">Personal Plan</Label>
  </RadioField>
  <RadioField>
    <Radio value="team" id="team" />
    <Label for="team">Team Plan</Label>
  </RadioField>
  <RadioField>
    <Radio value="enterprise" id="enterprise" />
    <Label for="enterprise">Enterprise Plan</Label>
  </RadioField>
</RadioGroup>
```

  </DocsTab>
</DocsTabs>

## Usage

```svelte
<script lang="ts">
  import { RadioGroup, Radio, RadioField } from '$lib/components/radio-group';
  import { Label, Description } from '$lib/components/fieldset';

  let selectedPlan = 'personal';
</script>
```

```svelte
<RadioGroup value={selectedPlan}>
  <RadioField>
    <Radio value="personal" id="personal" />
    <Label for="personal">Personal Plan</Label>
  </RadioField>
  <RadioField>
    <Radio value="team" id="team" />
    <Label for="team">Team Plan</Label>
  </RadioField>
  <RadioField>
    <Radio value="enterprise" id="enterprise" />
    <Label for="enterprise">Enterprise Plan</Label>
  </RadioField>
</RadioGroup>
```

## Properties

These are the properties specific to our `radio-group` implementation. For the complete list of properties, please refer to their <a href="https://www.bits-ui.com/docs/components/radio-group" target="_blank">documentation</a>.

### RadioGroup

<TypeTable
  type={{
    error: {
      description: "Whether the radio group has an error.",
      type: "boolean",
      default: "false",
    },
  }}
/>

## Examples

### Description

Use the `description` component inside the `RadioField` to add a description to the radio item.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/radio-group/description" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { RadioGroup, Radio, RadioField } from '$lib/components/radio-group';
  import { Label, Description } from '$lib/components/fieldset';

  let selectedPlan = 'personal';
</script>

<RadioGroup value={selectedPlan}>
  <RadioField>
    <Radio value="personal" id="personal" />
    <Label for="personal">Personal Plan</Label>
    <Description>Perfect for individuals and small projects</Description>
  </RadioField>
  <RadioField>
    <Radio value="team" id="team" />
    <Label for="team">Team Plan</Label>
    <Description>Great for teams up to 20 people</Description>
  </RadioField>
  <RadioField>
    <Radio value="enterprise" id="enterprise" />
    <Label for="enterprise">Enterprise Plan</Label>
    <Description>For large organizations with custom needs</Description>
  </RadioField>
</RadioGroup>
```

  </DocsTab>
</DocsTabs>

### Sublabel

Use the `sublabel` component inside the `RadioField` to add a sublabel to the radio item.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/radio-group/sublabel" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { RadioGroup, Radio, RadioField } from '$lib/components/radio-group';
  import { Label, Sublabel } from '$lib/components/fieldset';

  let selectedPlan = 'personal';
</script>

<RadioGroup value={selectedPlan}>
  <RadioField>
    <Radio value="personal" id="personal" />
    <Label for="personal">Personal Plan</Label>
    <Sublabel>1 license</Sublabel>
  </RadioField>
  <RadioField>
    <Radio value="team" id="team" />
    <Label for="team">Team Plan</Label>
    <Sublabel>10 licenses</Sublabel>
  </RadioField>
  <RadioField>
    <Radio value="enterprise" id="enterprise" />
    <Label for="enterprise">Enterprise Plan</Label>
    <Sublabel>Unlimited licenses</Sublabel>
  </RadioField>
</RadioGroup>
```

  </DocsTab>
</DocsTabs>

### Orientation

Use the `orientation` property to change the orientation of the `RadioGroup` component.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/radio-group/orientation" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { RadioGroup, Radio, RadioField } from '$lib/components/radio-group';
  import { Label } from '$lib/components/fieldset';

  let selectedPlan = 'personal';
</script>

<RadioGroup orientation="horizontal" value={selectedPlan}>
  <RadioField>
    <Radio value="personal" id="personal" />
    <Label for="personal">Personal Plan</Label>
  </RadioField>
  <RadioField>
    <Radio value="team" id="team" />
    <Label for="team">Team Plan</Label>
  </RadioField>
  <RadioField>
    <Radio value="enterprise" id="enterprise" />
    <Label for="enterprise">Enterprise Plan</Label>
  </RadioField>
</RadioGroup>
```

  </DocsTab>
</DocsTabs>

### Error

Use the `error` property to add an error state to the `RadioGroup` component.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/radio-group/error" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { RadioGroup, Radio, RadioField } from '$lib/components/radio-group';
  import { Label } from '$lib/components/fieldset';

  let selectedPlan = 'personal';
</script>

<RadioGroup error value={selectedPlan}>
  <RadioField>
    <Radio value="personal" id="personal" />
    <Label for="personal">Personal Plan</Label>
  </RadioField>
  <RadioField>
    <Radio value="team" id="team" />
    <Label for="team">Team Plan</Label>
  </RadioField>
  <RadioField>
    <Radio value="enterprise" id="enterprise" />
    <Label for="enterprise">Enterprise Plan</Label>
  </RadioField>
</RadioGroup>
```

  </DocsTab>
</DocsTabs>

### Disabled

Use the `disabled` property to disable the `RadioGroup` component.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/radio-group/disabled" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { RadioGroup, Radio, RadioField } from '$lib/components/radio-group';
  import { Label } from '$lib/components/fieldset';

  let selectedPlan = 'personal';
</script>

<RadioGroup disabled value={selectedPlan}>
  <RadioField>
    <Radio value="personal" id="personal" />
    <Label for="personal">Personal Plan</Label>
  </RadioField>
  <RadioField>
    <Radio value="team" id="team" />
    <Label for="team">Team Plan</Label>
  </RadioField>
  <RadioField>
    <Radio value="enterprise" id="enterprise" />
    <Label for="enterprise">Enterprise Plan</Label>
  </RadioField>
</RadioGroup>
```

  </DocsTab>
</DocsTabs>

### Disabled item

Use the `disabled` property on individual `Radio` components to disable specific radio items.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/radio-group/disabled-item" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { RadioGroup, Radio, RadioField } from '$lib/components/radio-group';
  import { Label } from '$lib/components/fieldset';

  let selectedPlan = 'personal';
</script>

<RadioGroup value={selectedPlan}>
  <RadioField>
    <Radio value="personal" id="personal" />
    <Label for="personal">Personal Plan</Label>
  </RadioField>
  <RadioField>
    <Radio value="team" id="team" />
    <Label for="team">Team Plan</Label>
  </RadioField>
  <RadioField>
    <Radio disabled value="enterprise" id="enterprise" />
    <Label for="enterprise">Enterprise Plan</Label>
  </RadioField>
</RadioGroup>
```

  </DocsTab>
</DocsTabs>
