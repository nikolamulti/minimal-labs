---
title: Radio group
description: Form control that allows users to select one option from a set of choices.
---

import { SvelteComponent } from "@/components/SvelteComponent";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import { Step, Steps } from "fumadocs-ui/components/steps";
import { TypeTable } from "fumadocs-ui/components/type-table";

<SvelteComponent url="/radio-group" />

```svelte
<script>
 import { RadioGroup } from '@/components/radio-group';
</script>

<RadioGroup value="option2">
	<Radio value="option1" id="option1">
		<RadioContent>
			<Label for="option1">Option 1</Label>
			<RadioSublabel>Sublabel</RadioSublabel>
		</RadioContent>
		<Helper>This is a helper text</Helper>
	</Radio>
</RadioGroup>
```

## Component API

### Root

<TypeTable
  type={{
    value: {
      description:
        "The controlled value of the radio group. Must match one of the radio items' values.",
      type: "string",
      default: "undefined",
    },
    orientation: {
      description: "Defines the layout direction of radio items.",
      type: "'horizontal' | 'vertical'",
      default: "vertical",
    },
    disabled: {
      description:
        "When true, disables all radio items in the group from being interacted with.",
      type: "boolean",
      default: "false",
    },
    required: {
      description:
        "When true, indicates that user must select an option for form validation.",
      type: "boolean",
      default: "false",
    },
    loop: {
      description:
        "When true, keyboard navigation will loop from last to first item and vice versa.",
      type: "boolean",
      default: "false",
    },
    error: {
      description:
        "When true, displays radio items in error state with error styling.",
      type: "boolean",
      default: "false",
    },
    onValueChange: {
      description:
        "Callback fired when the value changes. Receives the new value as an argument.",
      type: "function",
      default: "undefined",
    },
    asChild: {
      description:
        "When true, allows composing the component with other elements.",
      type: "boolean",
      default: "undefined",
    },
    groupEl: {
      description: "Reference to the root DOM element of the radio group.",
      type: "HTMLElement",
      default: "undefined",
    },
  }}
/>

>

### Item

<TypeTable
  type={{
    value: {
      description:
        "The unique value associated with this radio item. Used for selection and form submission.",
      type: "string",
      default: "''",
    },
    disabled: {
      description:
        "When true, prevents this specific radio item from being selected.",
      type: "boolean",
      default: "false",
    },
    buttonEl: {
      description: "Defines the layout direction of radio items.",
      type: "Reference to the radio button DOM element.",
      default: "undefined",
    },
    inputEl: {
      description:
        "Reference to the hidden input element used for form submission.",
      type: "boolean",
      default: "undefined",
    },
  }}
/>

>

## Events

RadioGroup.Item components emit the following events:

- `click` : Fired when the radio item is clicked
- `keydown` : Fired when a key is pressed while the radio item is focused
- `focus` : Fired when the radio item receives focus

## Orientation

The RadioGroup supports both vertical and horizontal layouts:

<SvelteComponent url="/radio-group/orientation" />

```svelte
<RadioGroup value="option2">
	<Radio value="option1" id="option1">
		<RadioContent>
			<Label for="option1">Option 1</Label>
			<RadioSublabel>Sublabel</RadioSublabel>
		</RadioContent>
		<Helper>This is a helper text</Helper>
	</Radio>
	<Radio value="option2" id="option2">
		<RadioContent>
			<Label for="option2">Option 2</Label>
			<RadioSublabel>Sublabel</RadioSublabel>
		</RadioContent>
  </Radio>
</RadioGroup>
```

## States

### Disabled

Individual items or the entire group can be disabled:

<SvelteComponent url="/radio-group/disabled" />

```svelte
<RadioGroup value="option2">
	<Radio disabled value="option1" id="option1">
		<RadioContent>
			<Label for="option1">Option 1</Label>
			<RadioSublabel>Sublabel</RadioSublabel>
		</RadioContent>
		<Helper>This is a helper text</Helper>
	</Radio>
</RadioGroup>
```

### Error

Supports error state for form validation:

<SvelteComponent url="/radio-group/error" />

```svelte
<RadioGroup error value="option2">
	<Radio value="option1" id="option1">
		<RadioContent>
			<Label for="option1">Option 1</Label>
			<RadioSublabel>Sublabel</RadioSublabel>
		</RadioContent>
		<Helper>This is a helper text</Helper>
	</Radio>
</RadioGroup>
```

## Accessibility

WFS lays on top of bits-ui and covers accessibility. For more information visit [documentation](https://bits-ui.com/docs/introduction).
