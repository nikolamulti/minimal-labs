---
title: Spinner
description: A loading indicator that provides visual feedback during asynchronous operations.
---

import { Svelte } from "@/components/svelte";
import { Tabs, Tab } from "@/components/tabs";
import { Step, Steps } from "@/components/steps";
import { TypeTable } from "@/components/type-table";
import { DocsButton } from "@/components/ui/docs-button";
import { DocsTabs, DocsTab } from "@/components/docs-tabs";

<div className="h-6" />

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/spinner" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Spinner } from '$lib/components/spinner';
</script>

<Spinner size={32} />
```

  </DocsTab>
</DocsTabs>

## Usage

```svelte
<script lang="ts">
  import { Progress } from '$lib/components/progress';

  let value = 70;
</script>
```

```svelte
<Progress bind:value variant="primary" />
```

## Properties

<TypeTable
  type={{
    size: {
      description: "Determines the size of the spinner.",
      type: "number",
      default: 24,
    },
  }}
/>

## Examples

### Sizes

Use the `size` property to change the size of the spinner.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/spinner/sizes" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Spinner } from '$lib/components/spinner';
</script>

<Spinner size={16} />
<Spinner size={24} />
<Spinner size={32} />
<Spinner size={48} />
```

  </DocsTab>
</DocsTabs>
