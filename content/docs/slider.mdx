---
title: Slider
description: A form field that allows users to select a single value from within a given range.
---

import { Svelte } from "@/components/svelte";
import { Tabs, Tab } from "@/components/tabs";
import { Step, Steps } from "@/components/steps";
import { TypeTable } from "@/components/type-table";
import { DocsButton } from "@/components/ui/docs-button";
import { DocsTabs, DocsTab } from "@/components/docs-tabs";

<DocsButton href="https://bits-ui.com/docs/components/slider">
  API Reference
</DocsButton>

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/slider" height={360} />
  </DocsTab>
  <DocsTab value="Code">
    
    
```svelte
<script lang="ts">
  import { Slider } from '$lib/components/slider';
  
  let value = [50];
</script>

<Slider bind:value />
```

  </DocsTab>
</DocsTabs>

## Usage

```svelte
<script lang="ts">
  import { Slider } from '$lib/components/slider';

  let value = [50];
</script>
```

```svelte
<Slider bind:value />
```

## Examples

### Range

Use an array with two values to create a range slider.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/slider/range" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Slider } from '$lib/components/slider';

  let value = [0, 50];
</script>

<Slider bind:value />
```

  </DocsTab>
</DocsTabs>

### Min max

Use the `min` and `max` properties to set the minimum and maximum values.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/slider/min-max" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Slider } from '$lib/components/slider';

  let value = [50];
</script>

<Slider bind:value min={0} max={100} />
```

  </DocsTab>
</DocsTabs>

### Step

Use the `step` property to set the step size.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/slider/step" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Slider } from '$lib/components/slider';

  let value = [50];
</script>

<Slider bind:value min={0} max={100} step={10} />
```

  </DocsTab>
</DocsTabs>

### Disabled

Use the `disabled` property to disable the `Slider` component.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/slider/disabled" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Slider } from '$lib/components/slider';

  let value = [50];
</script>

<Slider disabled bind:value />
```

  </DocsTab>
</DocsTabs>
