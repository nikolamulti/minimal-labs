---
title: Text input
description: Form field for single-line text entry.
---

import { Svelte } from "@/components/svelte";
import { Tabs, Tab } from "@/components/tabs";
import { Step, Steps } from "@/components/steps";
import { TypeTable } from "@/components/type-table";
import { DocsButton } from "@/components/ui/docs-button";
import { DocsTabs, DocsTab } from "@/components/docs-tabs";

<div className="h-6" />

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/text-input" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { TextInput } from '$lib/components/input';
</script>

<TextInput id="search" placeholder="Search" />
```

  </DocsTab>
</DocsTabs>

## Usage

```svelte
<script lang="ts">
  import { TextInput } from '$lib/components/input';
</script>
```

```svelte
<TextInput id="search" placeholder="Search" />
```

## Properties

<TypeTable
  type={{
    icon: {
      description: "The icon to be displayed in the input.",
      type: "ComponentType",
    },
    value: {
      description: "The value of the input.",
      type: "string",
    },
    placeholder: {
      description: "The placeholder text to display when empty.",
      type: "string",
    },
    disabled: {
      description: "Whether the input is disabled.",
      type: "boolean",
      default: "false",
    },
    error: {
      description: "Whether to show error styling.",
      type: "boolean",
      default: "false",
    },
  }}
/>

## Examples

### Icon support

Use the `icon` property to add an icon to the `TextInput` component.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/text-input/icon-support" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { TextInput } from '$lib/components/input';
  import { SearchDefault } from '$lib/components/icons';
</script>

<TextInput icon={SearchDefault} id="search" placeholder="Search" />
```

  </DocsTab>
</DocsTabs>

### Error

Use the `error` property to indicate that the input has an error.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/text-input/error" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { TextInput } from '$lib/components/input';
</script>

<TextInput error id="search" placeholder="Search" />
```

  </DocsTab>
</DocsTabs>

### Disabled

Use the `disabled` property to prevent users from interacting with the input.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/text-input/disabled" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { TextInput } from '$lib/components/input';
</script>

<TextInput disabled id="search" placeholder="Search" />
```

  </DocsTab>
</DocsTabs>
