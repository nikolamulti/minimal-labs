---
title: Switch
description: A toggle that allows users to turn an option on or off.
---

import { Svelte } from "@/components/svelte";
import { Tabs, Tab } from "@/components/tabs";
import { Step, Steps } from "@/components/steps";
import { TypeTable } from "@/components/type-table";
import { DocsButton } from "@/components/ui/docs-button";
import { DocsTabs, DocsTab } from "@/components/docs-tabs";

<DocsButton href="https://bits-ui.com/docs/components/switch">
  API Reference
</DocsButton>

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/switch" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Switch, SwitchField } from '$lib/components/switch';
  import { Label, Description } from '$lib/components/fieldset';

  let emailNotifications = false;
</script>

<SwitchField>
  <Switch id="notifications" name="notifications" bind:checked={emailNotifications} />
  <Label for="notifications">Email Notifications</Label>
  <Description>Get email updates about your account</Description>
</SwitchField>
```

  </DocsTab>
</DocsTabs>

## Usage

```svelte
<script lang="ts">
  import { Switch, SwitchField } from '$lib/components/switch';
  import { Label, Description } from '$lib/components/fieldset';

  let emailNotifications = false;
</script>
```

```svelte
<SwitchField>
  <Switch id="notifications" name="notifications" bind:checked={emailNotifications} />
  <Label for="notifications">Email Notifications</Label>
  <Description>Get email updates about your account</Description>
</SwitchField>
```

## Examples

### Disabled

Use the `disabled` property to prevent users from interacting with the switch.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/switch/disabled" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Switch, SwitchField } from '$lib/components/switch';
  import { Label, Description } from '$lib/components/fieldset';

  let emailNotifications = false;
</script>

<SwitchField>
  <Switch disabled id="notifications" name="notifications" bind:checked={emailNotifications} />
  <Label for="notifications">Email Notifications</Label>
  <Description>Get email updates about your account</Description>
</SwitchField>
```

  </DocsTab>
</DocsTabs>
