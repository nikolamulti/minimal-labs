---
title: Note
description: Provide contextual feedback messages for user actions.
---

import { Svelte } from "@/components/svelte";
import { Tabs, Tab } from "@/components/tabs";
import { Step, Steps } from "@/components/steps";
import { TypeTable } from "@/components/type-table";
import { DocsButton } from "@/components/ui/docs-button";
import { DocsTabs, DocsTab } from "@/components/docs-tabs";

<div className="h-6" />

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/note" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Note, NoteIcon, NoteContent, NoteTitle, NoteDescription, NoteLink } from '$lib/components/note';
  import { Updates } from '$lib/components/icons';
</script>

<Note variant="primary" style="outline" icon={Updates}>
  <NoteContent>
    <NoteTitle>New features available</NoteTitle>
    <NoteDescription>
      We've added dark mode support and improved performance across the application.
    </NoteDescription>
  </NoteContent>
  <NoteLink>See updates</NoteLink>
</Note>
```

  </DocsTab>
</DocsTabs>

## Usage

```svelte
<script lang="ts">
  import { Note, NoteIcon, NoteContent, NoteTitle, NoteDescription, NoteLink } from '$lib/components/note';
  import { Updates } from '$lib/components/icons';
</script>
```

```svelte
<Note variant="primary" style="outline" icon={Updates}>
  <NoteContent>
    <NoteTitle>New features available</NoteTitle>
    <NoteDescription>
      We've added dark mode support and improved performance across the application.
    </NoteDescription>
  </NoteContent>
  <NoteLink>See updates</NoteLink>
</Note>
```

## Properties

### Note

<TypeTable
  type={{
    variant: {
      description: "Determines the semantic meaning or purpose of the note.",
      type: "string",
      typeDescription: "'primary' | 'success' | 'warning' | 'critical'",
      default: "primary",
    },
    style: {
      description: "Determines the visual appearance within each variant.",
      type: "string",
      typeDescription: "'solid' | 'faded' | 'outline'",
      default: "outline",
    },
    orientation: {
      description: "The orientation of the note's content.",
      type: "string",
      typeDescription: "'vertical' | 'horizontal'",
      default: "vertical",
    },
    closable: {
      description: "Whether the note can be closed.",
      type: "boolean",
      default: "true",
    },
    fullWidth: {
      description:
        "Whether the note should take up the full width of its container.",
      type: "boolean",
      default: "false",
    },
    icon: {
      description: "The icon to display in the note.",
      type: "ComponentType",
    },
  }}
/>

### NoteLink

<TypeTable
  type={{
    href: {
      description: "The URL that the link points to.",
      type: "string",
    },
    leadingIcon: {
      description: "The leading icon of the link.",
      type: "ComponentType",
    },
    trailingIcon: {
      description: "The trailing icon of the link.",
      type: "ComponentType",
    },
    underline: {
      description: "Whether to show an underline on the link.",
      type: "boolean",
      default: "false",
    },
    button: {
      description: "Whether the link should behave like a button.",
      type: "boolean",
      default: "false",
    },
    disabled: {
      description:
        "Whether the link is disabled. Only works when used as a button.",
      type: "boolean",
      default: "false",
    },
  }}
/>

## Examples

### Variants

Use the `variant` property to change the color of the note, which helps convey its semantic meaning or purpose.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/note/variants" height={500} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Note, NoteIcon, NoteContent, NoteTitle, NoteDescription, NoteLink } from '$lib/components/note';
  import { Updates } from '$lib/components/icons';
</script>

<Note variant="primary" style="solid" icon={Updates}>
  <NoteContent>
    <NoteTitle>New features available</NoteTitle>
    <NoteDescription>
      We've added dark mode support and improved performance across the application.
    </NoteDescription>
  </NoteContent>
  <NoteLink>See updates</NoteLink>
</Note>

<Note variant="success" style="solid" icon={Updates}>
  <NoteContent>
    <NoteTitle>New features available</NoteTitle>
    <NoteDescription>
      We've added dark mode support and improved performance across the application.
    </NoteDescription>
  </NoteContent>
  <NoteLink>See updates</NoteLink>
</Note>

<Note variant="warning" style="solid" icon={Updates}>
  <NoteContent>
    <NoteTitle>New features available</NoteTitle>
    <NoteDescription>
      We've added dark mode support and improved performance across the application.
    </NoteDescription>
  </NoteContent>
	<NoteLink>See updates</NoteLink>
</Note>

<Note variant="critical" style="solid" icon={Updates}>
  <NoteContent>
    <NoteTitle>New features available</NoteTitle>
    <NoteDescription>
      We've added dark mode support and improved performance across the application.
    </NoteDescription>
  </NoteContent>
  <NoteLink>See updates</NoteLink>
</Note>
```

  </DocsTab>
</DocsTabs>

### Styles

Use the `style` property to change the visual appearance within each variant.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/note/styles" height={400} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Note, NoteIcon, NoteContent, NoteTitle, NoteDescription, NoteLink } from '$lib/components/note';
  import { Updates } from '$lib/components/icons';
</script>

<Note variant="primary" style="solid" icon={Updates}>
  <NoteContent>
    <NoteTitle>New features available</NoteTitle>
    <NoteDescription>
      We've added dark mode support and improved performance across the application.
    </NoteDescription>
  </NoteContent>
  <NoteLink>See updates</NoteLink>
</Note>

<Note variant="primary" style="faded" icon={Updates}>
  <NoteContent>
    <NoteTitle>New features available</NoteTitle>
    <NoteDescription>
      We've added dark mode support and improved performance across the application.
    </NoteDescription>
  </NoteContent>
  <NoteLink>See updates</NoteLink>
</Note>

<Note variant="primary" style="outline" icon={Updates}>
  <NoteContent>
    <NoteTitle>New features available</NoteTitle>
    <NoteDescription>
      We've added dark mode support and improved performance across the application.
    </NoteDescription>
  </NoteContent>
  <NoteLink>See updates</NoteLink>
</Note>
```

  </DocsTab>
</DocsTabs>

### Orientation

Use the `orientation` property to change the orientation of the note.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/note/orientation" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Note, NoteIcon, NoteContent, NoteTitle, NoteDescription, NoteLink } from '$lib/components/note';
  import { Updates } from '$lib/components/icons';
</script>

<Note variant="primary" style="outline" icon={Updates} orientation="horizontal">
  <NoteContent>
    <NoteTitle>New features available</NoteTitle>
    <NoteDescription>
      We've added dark mode support and improved performance across the application.
    </NoteDescription>
  </NoteContent>
  <NoteLink>See updates</NoteLink>
</Note>
```

  </DocsTab>
</DocsTabs>

### Full width

Use the `fullWidth` property to make the note take up the full width of its container.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/note/full-width" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Note, NoteIcon, NoteContent, NoteTitle, NoteDescription, NoteLink } from '$lib/components/note';
  import { Updates } from '$lib/components/icons';
</script>

<Note variant="primary" style="outline" icon={Updates} fullWidth={true}>
  <NoteContent>
    <NoteTitle>New features available</NoteTitle>
    <NoteDescription>
      We've added dark mode support and improved performance across the application.
    </NoteDescription>
  </NoteContent>
  <NoteLink>See updates</NoteLink>
</Note>
```

  </DocsTab>
</DocsTabs>

### Closable

Use the `closable` property to prevent the user from closing the note. By default, notes are closable.

<DocsTabs items={["Preview", "Code"]}>
  <DocsTab value="Preview" className="mt-6">
    <Svelte url="/note/closable" height={360} />
  </DocsTab>
  <DocsTab value="Code">

```svelte
<script lang="ts">
  import { Note, NoteIcon, NoteContent, NoteTitle, NoteDescription, NoteLink } from '$lib/components/note';
  import { Updates } from '$lib/components/icons';
</script>

<Note variant="primary" style="outline" icon={Updates} closable={false}>
  <NoteContent>
    <NoteTitle>New features available</NoteTitle>
    <NoteDescription>
      We've added dark mode support and improved performance across the application.
    </NoteDescription>
  </NoteContent>
  <NoteLink>See updates</NoteLink>
</Note>
```

  </DocsTab>
</DocsTabs>
